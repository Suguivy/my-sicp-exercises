;; The answer is produced in reverse order because the elements are picked up from the beggining of the list things and placed at the beginning of the list answer, and that produces a reversed list.

;; The procedure doesn't work because to build a list the first element of a pair must be the element, and not a list.
